server:
  port: 7070

spring:
  profiles:
    active: local

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: Cql Elm Translation Services

fhir:
  conversion:
    baseurl: ${FHIR_SERVICES_URL}