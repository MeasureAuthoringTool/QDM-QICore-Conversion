fhir:
  conversion:
    baseurl: ${FHIR_SERVICES_URL}

swagger-server: ${CQL_ELM_SWAGGER_URL}

logging:
  level:
    root: INFO
    gov.cms.mat.config.logging: WARN
  file:
    name: cql-elm-translation.log
    clean-history-on-start: true
    max-history: 1
  pattern:
    console: "%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %mdc %-5level %logger{36}.%M - %msg%n"
    file: "%d{dd-MM-yyyy HH:mm:ss.SSS} [%thread] %-5level %mdc %logger{36}.%M - %msg%n"

mat-api-key: ${MAT-API-KEY}